# 3D Models

Place the real eringi creature model here:

- `eringi_human.glb`

Current implementation expects this file and will render it in the 3D world layer.
If not available, use any compatible `.glb` model with that filename for now.
